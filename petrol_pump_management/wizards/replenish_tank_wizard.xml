<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Wizard Form View -->
        <record id="view_replenish_tank_wizard_form" model="ir.ui.view">
            <field name="name">replenish.tank.wizard.form</field>
            <field name="model">petrol.pump.replenish.tank.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <field name="tank_id" invisible="1"/>
                        <field name="quantity"/>
                    </group>
                    <footer>
                        <button name="action_replenish" string="Replenish" type="object" class="btn-primary"/>
                        <button string="Cancel" special="cancel" class="btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Wizard Action -->
        <record id="action_replenish_tank_wizard" model="ir.actions.act_window">
            <field name="name">Replenish Tank</field>
            <field name="res_model">petrol.pump.replenish.tank.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>
